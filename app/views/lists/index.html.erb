<h1>投稿一覧</h1>

<% @lists.each do |list| %>
<!--eachメソッドでオブジェクト内の要素をデータの先頭から順に取り出して処理を行う-->
<!--eachメソッドの書き方は下記の通り-->
  <!--（範囲、ハッシュ、配列などを指定）.each do |変数|-->
  <!--実行する処理-->


  <p>タイトル</p>
  <span><%= link_to list.title, list_path(list.id)%></span>
  <!--%で始まるタグはRubyで使われるタグ-->
  <!--＜% %＞はタグ内の結果がHTMLに変換されずに処理される-->
  <!--＜%= %＞はタグ内の結果がHTMLに変換される-->
  <!--＜%= link_to 表示させるテキスト , "リンク先URL" [,オプション] %＞-->

<% end %>
